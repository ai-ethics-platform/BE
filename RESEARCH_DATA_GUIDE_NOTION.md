# 📊 AI Ethics 실험 데이터 조회 가이드

## 🎯 개요

연구자들이 실험 데이터를 쉽게 조회하고 분석할 수 있도록 **웹 대시보드**와 **REST API**를 제공합니다.

---

## 🖥️ 웹 대시보드 (추천)

### 접속 방법

```
https://dilemmai-idl.com/research/dashboard
```

### 주요 기능

#### 1️⃣ 전체 통계 요약
- 총 사용자 수
- 동의한 사용자 수
- 총 방(Room) 수
- 시작된 게임 수
- 라운드 선택 데이터 수
- 음성 녹음 수

페이지 로드 시 자동으로 표시됩니다.

#### 2️⃣ 데이터 다운로드
**엑셀 파일 (Excel)**
- 모든 실험 데이터를 스프레드시트로 다운로드
- 사용자 정보, 게임 데이터, 선택 정보가 포함된 통합 파일
- 버튼 클릭만으로 즉시 다운로드

**JSON 파일**
- 프로그래밍 분석용 원시 데이터
- Python, R 등에서 바로 사용 가능

#### 3️⃣ 특정 방(Room) 조회
- Room ID를 입력하여 특정 게임 세션의 상세 정보 확인
- 참가자 목록, 라운드별 선택, 음성 녹음 정보 포함

#### 4️⃣ 특정 사용자 조회
- User ID를 입력하여 특정 사용자의 모든 참여 기록 확인
- 참여한 게임, 선택 패턴, 음성 녹음 이력 포함

#### 5️⃣ 선택 데이터 분석
- 라운드별 선택 분포 분석
- 역할별 선택 경향 분석
- 확신도(Confidence) 통계
- 주제별 필터링 가능

#### 6️⃣ 디버깅 정보
- 데이터베이스 상태 확인
- 테스트 데이터 검증

---

## 🔧 REST API (프로그래밍 접근)

### API 문서

```
https://dilemmai-idl.com/docs
```

Swagger UI에서 모든 API를 테스트할 수 있습니다.

### 주요 엔드포인트

#### 📊 통계 요약
```http
GET /research/experiments/summary
```

**응답 예시:**
```json
{
  "total_users": 277,
  "users_with_full_consent": 180,
  "total_rooms": 760,
  "total_started_rooms": 450,
  "total_round_choices": 1512,
  "total_voice_recordings": 890
}
```

#### 📥 엑셀 다운로드
```http
GET /research/experiments/export/excel
```

브라우저에서 직접 접속하면 엑셀 파일이 다운로드됩니다.

#### 📄 JSON 데이터 Export
```http
GET /research/experiments/export?limit=1000&started_only=true
```

**쿼리 파라미터:**
- `started_only`: 시작된 게임만 포함 (기본값: true)
- `with_consent_only`: 동의한 사용자만 포함 (기본값: true)
- `skip`: 건너뛸 개수 (페이징, 기본값: 0)
- `limit`: 가져올 최대 개수 (기본값: 100, 최대: 1000)

#### 🏠 특정 방 조회
```http
GET /research/experiments/rooms/{room_id}
```

**예시:**
```
GET /research/experiments/rooms/123
```

#### 👤 특정 사용자 조회
```http
GET /research/experiments/users/{user_id}
```

**예시:**
```
GET /research/experiments/users/456
```

#### 📈 선택 데이터 분석
```http
GET /research/experiments/choices/analysis?topic=자율주행
```

**쿼리 파라미터:**
- `topic`: 주제별 필터링 (선택사항)

**응답 예시:**
```json
{
  "round_choices": [
    {
      "round_number": 1,
      "choice": 0,
      "count": 45,
      "avg_confidence": 3.8
    }
  ],
  "role_choices": [
    {
      "role_id": "DEV",
      "choice": 1,
      "count": 23
    }
  ]
}
```

#### 🔍 디버깅 정보
```http
GET /research/experiments/debug/counts
```

---

## 📋 데이터 구조 설명

### 사용자 정보
- `user_id`: 사용자 고유 ID
- `username`: 사용자명
- `email`: 이메일
- `birthdate`: 생년월일
- `gender`: 성별
- `education_level`: 학력
- `major`: 전공
- `data_consent`: 데이터 사용 동의 여부
- `voice_consent`: 음성 녹음 동의 여부

### 게임 세션 정보
- `room_id`: 방 고유 ID
- `room_code`: 6자리 참여 코드
- `title`: 방 제목
- `topic`: 토픽 (딜레마 주제)
- `ai_type`: AI 유형
- `is_started`: 게임 시작 여부
- `participants`: 참가자 목록

### 라운드 선택 정보
- `round_number`: 라운드 번호 (1-5)
- `choice`: 선택 (0 또는 1)
- `subtopic`: 서브토픽 (역할별 구체적 상황)
- `confidence`: 확신도 (1-5)
- `role_id`: 역할 ID

### 음성 녹음 정보
- `voice_session_id`: 음성 세션 ID
- `user_id` / `guest_id`: 사용자 식별자
- `file_path`: 녹음 파일 경로
- `file_size`: 파일 크기
- `duration`: 녹음 시간
- `is_processed`: 처리 완료 여부

---

## 🔐 보안 및 권한

### 현재 접근 제어
- 모든 연구 API는 **인증 없이** 접근 가능합니다.
- 프로덕션 환경에서는 **연구자 전용 계정**을 통한 접근만 허용해야 합니다.

### 권장 보안 조치 (향후)
1. JWT 인증 추가
2. 연구자 전용 role-based access control (RBAC)
3. IP 화이트리스트
4. API Rate Limiting

---

## 💡 활용 예시

### Python에서 데이터 분석

```python
import requests
import pandas as pd

# 통계 가져오기
response = requests.get("https://dilemmai-idl.com/research/experiments/summary")
stats = response.json()
print(f"총 사용자: {stats['total_users']}")

# JSON 데이터 다운로드
response = requests.get("https://dilemmai-idl.com/research/experiments/export?limit=1000")
data = response.json()

# pandas DataFrame으로 변환
rooms = data['rooms']
df = pd.json_normalize(rooms)

# 분석 예시
print(df['topic'].value_counts())
```

### R에서 데이터 분석

```r
library(httr)
library(jsonlite)

# 데이터 가져오기
response <- GET("https://dilemmai-idl.com/research/experiments/export?limit=1000")
data <- fromJSON(content(response, "text"))

# 데이터프레임 변환
rooms <- data$rooms

# 분석 예시
table(rooms$topic)
```

### 엑셀에서 직접 분석
1. 웹 대시보드에서 "엑셀 다운로드" 버튼 클릭
2. Excel, Google Sheets, LibreOffice 등에서 열기
3. 피벗 테이블, 차트 등으로 분석

---

## 🚨 문제 해결

### 데이터가 비어있을 때
1. `/research/experiments/debug/counts`로 실제 데이터 확인
2. 필터 옵션 조정 (`started_only=false`, `with_consent_only=false`)

### API 오류가 발생할 때
1. 브라우저 콘솔에서 에러 메시지 확인
2. API 문서(`/docs`)에서 요청 형식 확인
3. 백엔드 로그 확인

### 엑셀 다운로드가 안 될 때
1. 브라우저 팝업 차단 해제
2. 직접 URL 접속: `https://dilemmai-idl.com/research/experiments/export/excel`

---

## 📞 지원

문제가 발생하거나 추가 기능이 필요한 경우:
1. GitHub Issues에 문의
2. 백엔드 개발자에게 연락
3. API 문서(`/docs`)의 "Try it out" 기능으로 테스트

---

## 🎨 대시보드 UI 개선 (구현 완료)

### 구현 내용
- **반응형 디자인**: 모바일, 태블릿, 데스크톱 모두 지원
- **직관적 인터페이스**: 버튼 클릭만으로 데이터 조회
- **실시간 피드백**: 로딩, 성공, 에러 상태 표시
- **JSON 미리보기**: 데이터 구조를 바로 확인 가능
- **자동 통계 로딩**: 페이지 접속 시 자동으로 통계 표시

### 기술 스택
- **Frontend**: Pure HTML5 + CSS3 + JavaScript (ES6+)
- **Backend**: FastAPI + AsyncSession
- **Database**: MySQL
- **UI Framework**: Custom CSS with Gradient Design

---

## 📊 데이터 활용 시나리오

### 1. 연구 논문 작성
- 통계 요약으로 기술 통계 작성
- 선택 분석으로 패턴 파악
- 역할별 선택 경향으로 인사이트 도출

### 2. 실험 모니터링
- 실시간 참여자 수 확인
- 데이터 수집 진행 상황 체크
- 동의율 추적

### 3. 데이터 품질 검증
- 디버깅 정보로 데이터 완전성 확인
- 누락된 데이터 파악
- 테스트 데이터 식별 및 제거

### 4. 후속 연구 설계
- 토픽별 참여율 분석
- 역할별 선택 패턴 파악
- 음성 데이터 가용성 확인

---

## ✅ 체크리스트

### 연구 시작 전
- [ ] 대시보드 접속 테스트
- [ ] 엑셀 다운로드 확인
- [ ] API 접근 권한 확인

### 데이터 수집 중
- [ ] 주기적으로 통계 확인
- [ ] 동의율 모니터링
- [ ] 음성 녹음 상태 체크

### 연구 종료 후
- [ ] 전체 데이터 다운로드
- [ ] 데이터 백업
- [ ] 품질 검증 수행

---

**마지막 업데이트:** 2026-01-27
**버전:** 1.0.0
